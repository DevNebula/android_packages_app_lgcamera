package com.lge.camera.components;

import android.content.Context;
import android.util.AttributeSet;
import android.widget.ListView;

public class FastThumbnailListView extends ListView {
    public FastThumbnailListView(Context context) {
        super(context);
    }

    public FastThumbnailListView(Context context, AttributeSet attrs) {
        super(context, attrs);
    }
}
